<li>
	{{burger_name}}

	<button class="change-Devoured" data-id="{{id}}" data-new-Devoured=""="{{Devoured}}">
		{{#if Devoured}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{id}}">DELETE!</button>
</li>
